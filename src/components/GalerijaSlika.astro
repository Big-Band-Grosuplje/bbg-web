---
import { Image, getImage } from 'astro:assets';
import { podnapisZa, slikaZa, type Fotografija } from '../lib/galerija';
import { slovar, type Jezik } from '../i18n';

interface Props {
  foto: Fotografija;
  jezik: Jezik;
}
const { foto, jezik } = Astro.props;
const t = slovar(jezik);
const podnapis = podnapisZa(foto, jezik);

const vir = slikaZa(foto.datoteka);
/* Za lightbox pripravimo večjo različico iste fotografije. */
const velika = await getImage({ src: vir, width: 1600, format: 'webp' });
---

<li class="slika">
  <figure>
    <button
      type="button"
      class="slika__gumb"
      data-slika={velika.src}
      data-opis={podnapis}
      aria-label={`${t.lightbox.povecaj}: ${podnapis}`}
    >
      <Image src={vir} alt={podnapis} width={800} loading="lazy" />
    </button>
    <figcaption>{podnapis}</figcaption>
  </figure>
</li>

<style>
  .slika figure {
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 10px;
  }
  .slika__gumb {
    display: block;
    padding: 0;
    border: 0;
    background: none;
    cursor: zoom-in;
    line-height: 0;
  }
  .slika__gumb:focus-visible {
    outline: 2px solid var(--bbg-obroba-mocna);
    outline-offset: 3px;
  }
  .slika img {
    width: 100%;
    height: auto;
    display: block;
    border-radius: var(--bbg-r-l);
    border: 2.5px solid var(--bbg-gold);
  }
  .slika figcaption {
    font-size: 13.5px;
    line-height: 1.5;
    opacity: 0.78;
  }
</style>
